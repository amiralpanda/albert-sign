ATOME_API_KEY=
BLOB_READ_WRITE_TOKEN=

# Mail (optional — invitations can use signUrl only with sendEmail: false)
# Preferred on Vercel: Google Workspace SMTP (no Resend account needed)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jeremy@atome.sh
SMTP_PASS=
SMTP_SECURE=false

# Or Resend (optional)
RESEND_API_KEY=
RESEND_FROM=Jérémy Foucray <jeremy@atome.sh>
SIGNING_FROM=Jérémy Foucray <jeremy@atome.sh>
SIGNING_INVITER_NAME=Jérémy Foucray
SIGNING_COMPLETION_CC=finance@atome.sh
SIGNING_PUBLIC_BASE_URL=http://localhost:5173
# Internal preview job auth (set on Vercel production)
SIGNING_JOB_SECRET=
